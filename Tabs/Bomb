-- BOMB GLOVE TAB SCRIPT

if not getgenv().sbstew_Utils then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/stewingit/sbstew/refs/heads/main/Stuff"))()
end

local Stewfield = getgenv().sbstew_Stewfield
local Window = getgenv().sbstew_Window
local Utils = getgenv().sbstew_Utils
local RLibrary = getgenv().sbstew_Remotes

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

-- Utilizing variables from Utils
local ZeroPosition = Utils.GetZeroPosition()
local BotSpeed = Utils.BotSpeed
local RoamRange = Utils.RoamRange
local MaxLockDistance = Utils.MaxLockDistance
local AcceptableDistance = Utils.AcceptableDistance
local TargetGlove = "Warp"

local SlapRemote = RLibrary.Slap.Warp
local AbilityRemote = RLibrary.Ability.Warp
local SlapAnimation = ReplicatedStorage.Slap

local LockedTarget = nil
local SlapCooldown = false
local LocalHumanoid = nil
local Animation = nil
local HasSlapped = false

local BombTab = Window:CreateTab("Bomb Glove", "Bomb")
BombTab:CreateLabel("Bomb Glove Obtainment")

local function CharacterLoaded(Character)
    local Hum, Anim = Utils.SetupBotSupport(Character, SlapAnimation)
    LocalHumanoid = Hum
    Animation = Anim
    HasSlapped = false
end

local function GetTarget(LocalHumanoid)
    for _, v in ipairs(Players:GetPlayers()) do
        local Character = v.Character
        if v ~= LocalPlayer and Character and not LocalPlayer:IsFriendsWith(v.UserId) then
            local Humanoid = Character:FindFirstChildWhichIsA("Humanoid")
            if Humanoid and Humanoid.Health > 0 then
                local Root = Humanoid.RootPart
                if Root then
                    local Distance = (LocalHumanoid.RootPart.Position - Root.Position).Magnitude
                    local InMap = (Root.Position - ZeroPosition).Magnitude < RoamRange
                    
                    if InMap and Distance <= MaxLockDistance and Visible and Utils.IsTargetValid(Character) then
                        return Root, Distance
                    end
                end
            end
        end
    end
    return nil, 512
end

local function StartBombLoop()
    task.spawn(function()
        while getgenv().BSF_Bomb do
            local Character = LocalPlayer.Character
            if LocalHumanoid and LocalHumanoid.Parent and Character then
                
                -- 1. CHECK LOBBY / EQUIP
                if Utils.IsInLobby(Character) then
                    local _, currentGlove = Utils.GetStats()
                    if currentGlove ~= TargetGlove then
                        Utils.EquipGlove(TargetGlove)
                    else
                        Utils.WalkToPortal()
                    end
                
                -- 2. ARENA LOGIC
                elseif not HasSlapped then
                    local TargetRoot, Distance = GetTarget(LocalHumanoid)
                    if TargetRoot then
                        LocalHumanoid:MoveTo(TargetRoot.Position)
                        if Distance < AcceptableDistance and not SlapCooldown then
                            SlapCooldown = true
                            Animation:Play()
                            SlapRemote:FireServer(TargetRoot)
                            HasSlapped = true
                            task.delay(0.6, function() SlapCooldown = false end)
                        end
                    end
                
                -- 3. ABILITY & BARRIER
                else
                    task.wait(2)
                    
                    -- Fire Ability
                    AbilityRemote:FireServer({})
                    
                    task.wait(0.1)
                    
                    -- Instant Death Barrier Touch
                    local leg = Character:FindFirstChild("Left Leg") or LocalHumanoid.RootPart
                    local barrier = Workspace:FindFirstChild("DEATHBARRIER") or Workspace:FindFirstChild("dedBarrier")
                    
                    if barrier then
                        firetouchinterest(leg, barrier, 0)
                        task.wait(0.05)
                        firetouchinterest(leg, barrier, 1)
                    end
                    
                    getgenv().BSF_Bomb = false -- Stop script
                end

                -- Maintain Speed
                if LocalHumanoid.RootPart then
                    LocalHumanoid.WalkSpeed = BotSpeed
                end
            end
            RunService.Heartbeat:Wait()
        end
    end)
end

BombTab:CreateButton({
    Name = "Equip Warp glove",
    Callback = function()
        local unlocked = LocalPlayer:FindFirstChild("_unlockedGloves")
        local hasWarp = unlocked and unlocked:FindFirstChild("Warp") and unlocked.Warp.Value == true

        if Utils.IsInLobby() then
            if hasWarp then
                Utils.EquipGlove("Warp")
            else
                Stewfield:Notify({
                    Title = "Missing Requirement",
                    Content = "You need the Warp glove unlocked first!",
                    Duration = 5,
                    Image = "lock"
                })
            end
        else
            Stewfield:Notify({
                Title = "Error",
                Content = "Make sure you're in the lobby.",
                Duration = 5,
                Image = "circle-alert"
            })
        end
    end,
})

BombTab:CreateButton({
    Name = "Get Bomb",
    Callback = function()
        local _, currentGlove = Utils.GetStats()

        if currentGlove == "Warp" then
            getgenv().BSF_Bomb = true
            if LocalPlayer.Character then
                CharacterLoaded(LocalPlayer.Character)
                StartBombLoop()
            end
        else
            Stewfield:Notify({
                Title = "Glove Required",
                Content = "You must have the Warp glove equipped first!",
                Duration = 5,
                Image = "lock"
            })
        end
    end,
})

BombTab:CreateParagraph({
    Title = "Instructions",
    Content = "1. Press 'Equip Warp glove'\n2. Press 'Get Bomb' to start the bot."
})

return BombTab
