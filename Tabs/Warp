-- WARP GLOVE TAB SCRIPT
if not getgenv().sbstew_Utils then
    loadstring(game:HttpGet("https://raw.githubusercontent.com/stewingit/sbstew/refs/heads/main/Stuff"))()
end

local Stewfield = getgenv().sbstew_Stewfield
local Window = getgenv().sbstew_Window
local Places = getgenv().sbstew_Places
local RLibrary = getgenv().sbstew_Remotes
local Utils = getgenv().sbstew_Utils

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

-- Utilizing shared values from Utils
local ZeroPosition = Utils.GetZeroPosition()
local RoamRange = Utils.RoamRange
local MaxLockDistance = Utils.MaxLockDistance
local AcceptableDistance = Utils.AcceptableDistance
local TargetGlove = "Swapper"

-- Pulled from the Global Remote Library
local SlapRemote = RLibrary.Slap.Swapper
local AbilityRemote = RLibrary.Ability.Swapper
local SlapAnimation = ReplicatedStorage.Slap
local Reset = RLibrary.Misc.Reset

local LockedTarget = nil
local SlapCooldown = false
local LocalHumanoid = nil
local Animation = nil
local InfiniteMode = false

local WarpTab = Window:CreateTab("Warp Glove", "rss")
WarpTab:CreateLabel("Warp Glove Obtainment")

-- Utilizing SetupBotSupport from Stuff Script
local function CharacterLoaded(Character)
    local Hum, Anim = Utils.SetupBotSupport(Character, SlapAnimation)
    LocalHumanoid = Hum
    Animation = Anim
end

local function GetTarget()
    if LockedTarget then
        local Character = LockedTarget.Character
        local IsValid = LockedTarget.Parent == Players and Character and Character:FindFirstChild("Humanoid") and Character.Humanoid.Health > 0

        local outOfRange = false
        if IsValid then
            local Root = Character.Humanoid.RootPart
            if Root and (Root.Position - ZeroPosition).Magnitude >= RoamRange then
                outOfRange = true
            end
        end

        if not IsValid or outOfRange then
            getgenv().BSF = false
            LockedTarget = nil

            task.spawn(function()
                -- Stop movement immediately
                if LocalHumanoid and LocalHumanoid.RootPart then
                    LocalHumanoid:MoveTo(LocalHumanoid.RootPart.Position)
                end
                
                task.wait(0.35)
                if AbilityRemote then AbilityRemote:FireServer() end
                task.wait(1)
                local ResetRemote = Reset()
                if ResetRemote then ResetRemote:FireServer() end
                Utils.StopSafetyLoop() -- Deactivate once reset is handled
            end)

            return nil, 512
        end

        local Root = Character.Humanoid.RootPart
        return LockedTarget, (LocalHumanoid.RootPart.Position - Root.Position).Magnitude
    end

    for _, v in ipairs(Players:GetPlayers()) do
        local Character = v.Character
        if v ~= LocalPlayer and Character and not LocalPlayer:IsFriendsWith(v.UserId) then
            local Humanoid = Character:FindFirstChildWhichIsA("Humanoid")
            if Humanoid and Humanoid.Health > 0 then
                local Root = Humanoid.RootPart
                if Root then
                    local Distance = (LocalHumanoid.RootPart.Position - Root.Position).Magnitude
                    local InMap = (Root.Position - ZeroPosition).Magnitude < RoamRange
                    
                    -- Using centralized target validation from Stuff Script
                    if InMap and Distance <= MaxLockDistance and Utils.IsTargetValid(Character) then
                        LockedTarget = v
                        return v, Distance
                    end
                end
            end
        end
    end

    return nil, 512
end

local function StartLoops()
    task.spawn(function()
        while getgenv().BSF do
            local Character = LocalPlayer.Character
            if LocalHumanoid and LocalHumanoid.RootPart and Character then
                if not Utils.IsInLobby(Character) then
                    local TargetPlayer, Distance = GetTarget()

                    if TargetPlayer and TargetPlayer.Character then
                        local TargetRoot = TargetPlayer.Character:FindFirstChildWhichIsA("Humanoid").RootPart
                        if LocalHumanoid.Health > 0 and TargetRoot then
                            local TargetPosition = TargetRoot.Position + Utils.ReturnValidVector3Unit(TargetRoot.AssemblyLinearVelocity)
                            LocalHumanoid:MoveTo(TargetPosition)

                            if Distance < AcceptableDistance and not SlapCooldown then
                                SlapCooldown = true
                                Animation:Play()
                                if SlapRemote then SlapRemote:FireServer(TargetRoot) end
                                task.delay(0.6, function() SlapCooldown = false end)
                            end
                        end
                    else
                        LocalHumanoid:MoveTo(LocalHumanoid.RootPart.Position)
                    end
                else
                    local _, currentGlove = Utils.GetStats()
                    if currentGlove ~= TargetGlove then
                        Utils.EquipGlove(TargetGlove)
                    else
                        Utils.WalkToPortal()
                    end
                end
            end
            RunService.RenderStepped:Wait()
        end
        Utils.StopSafetyLoop() -- Stop safety loop if BSF main loop breaks
    end)

    task.spawn(function()
        while task.wait(0.3) do
            if not getgenv().BSF then 
                Utils.StopSafetyLoop()
                break 
            end
            local Character = LocalPlayer.Character

            if LocalHumanoid and Character then
                local inLobby = Utils.IsInLobby(Character)
            
                if inLobby then
                    Utils.StopSafetyLoop()
                else
                    Utils.StartSafetyLoop()
                    for _, v in ipairs(Character:GetChildren()) do
                        if v:IsA("BasePart") then v.CanTouch = false end
                    end
                end

                if not Character:FindFirstChildWhichIsA("Tool") and LocalPlayer.Backpack:FindFirstChildWhichIsA("Tool") then
                    LocalPlayer.Backpack:FindFirstChildWhichIsA("Tool").Parent = Character
                end
            end
        end
    end)
end

LocalPlayer.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    if InfiniteMode then
        getgenv().BSF = true
        CharacterLoaded(char)
        StartLoops()
    end
end)

WarpTab:CreateButton({
    Name = "Equip Swapper glove",
    Callback = function()
        if Utils.IsInLobby() then
            local slaps, _ = Utils.GetStats()
            if slaps >= 350 then
                Utils.EquipGlove("Swapper")
            else
                Stewfield:Notify({
                    Title = "Insufficient Slaps",
                    Content = "You need 350 slaps to equip Swapper!",
                    Duration = 5,
                    Image = "lock"
                })
            end
        else
            Stewfield:Notify({
                Title = "Error",
                Content = "Make sure you're in the lobby.",
                Duration = 5,
                Image = "circle-alert"
            })
        end
    end,
})

WarpTab:CreateButton({
    Name = "Get Warp",
    Callback = function()
        local _, currentGlove = Utils.GetStats()

        if currentGlove == "Swapper" then
            getgenv().BSF = true
            if LocalPlayer.Character then
                CharacterLoaded(LocalPlayer.Character)
                StartLoops()
            end
        else
            Stewfield:Notify({
                Title = "Glove Required",
                Content = "You must have the Swapper glove equipped first!",
                Duration = 5,
                Image = "lock"
            })
        end
    end,
})

WarpTab:CreateToggle({
    Name = "Retry",
    CurrentValue = false,
    Flag = "AutoWarp_Flag",
    Callback = function(Value)
        InfiniteMode = Value
        if not Value then
            getgenv().BSF = false -- Kill loops
            Utils.StopSafetyLoop()
        end
    end,
})

WarpTab:CreateParagraph({
    Title = "Instructions",
    Content = "1. Press 'Equip Swapper'\n2. Press 'Get Warp' to start the bot logic.\n3. If 'Retry' is enabled, the bot will automatically restart.\n4. If 'Retry' is disabled, the bot will stop after the first attempt or reset."
})

return WarpTab
